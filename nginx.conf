# ============================================
# 🔹 GŁÓWNA STRONA: tatajestwazny.pl
# ============================================
server {
    listen 80;
    server_name tatajestwazny.pl www.tatajestwazny.pl;

    root /var/www/html/public;
    index index.php index.html;

    access_log /var/log/nginx/tatajestwazny.access.log;
    error_log /var/log/nginx/tatajestwazny.error.log;

    location / {
        try_files $uri $uri/ /index.php$is_args$args;
    }

    location ~ \.php$ {
        include fastcgi_params;
        fastcgi_pass tjw_php:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
    }

    location ~ /\.ht {
        deny all;
    }
}


# ============================================
# ✅ APLIKACJA: app.tatajestwazny.pl
# ============================================
server {
    listen 80;
    server_name app.tatajestwazny.pl;

    root /var/www/html/app/public;
    index index.php index.html;

    access_log /var/log/nginx/tatajestwazny-app.access.log;
    error_log /var/log/nginx/tatajestwazny-app.error.log;

    location / {
        try_files $uri $uri/ /index.php$is_args$args;
    }

    location ~ \.php$ {
        include fastcgi_params;
        fastcgi_pass tjw_php:9000;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_path_info;
    }

    location ~ /\.ht {
        deny all;
    }
}
